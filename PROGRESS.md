# PROGRESS

Update this file after every completed step. Read it first, every session,
before doing anything else.

## Current status
**Phase:** 1 — Backend — DONE (all "Done when" criteria met)
**Next step:** Phase 2, step 1 — Pick your ~200-company universe (e.g. S&P 500 constituents), store the list (do not start until confirmed)

## Log
- [x] Phase 0, step 1 — Project scaffolded: `npm init`, TypeScript, `ts-node`, `@types/node`, `tsconfig.json` generated. (Re-verified/redone 2026-09-09 — the scaffold files were missing from disk despite being checked off, so `npm init`, dev-dep install, and `tsc --init` were re-run before continuing.)
- [x] Phase 0, step 2 — git initialized, `.gitignore` added (`node_modules/`, `dist/`, `build/`, logs, `.env`/`.env.*` except `.env.example`), first commit made (later squashed to `55d6cf3` to drop a commit trailer the user didn't want).
- [x] Phase 0, step 3 — GitHub repo created (`IshanJ9/EDGAR-Radar`), remote `origin` added, pushed to `Main`.
- [x] Phase 0, step 4 — `index.ts` accepts a CIK CLI arg, pads it to 10 digits, and calls `data.sec.gov/api/xbrl/companyfacts/CIK{...}.json` with `User-Agent: EDGAR Radar (<email>)` read from `.env` via `dotenv` (see `.env.example`). Verified against CIK 320193 (Apple) — 200 response, printed `entityName`. Also fixed: `typescript` had resolved to the new v7 native compiler, which isn't yet supported by `ts-node`; pinned to `^5.7` to fix.
- [x] Phase 0, step 5 — `mostRecentFact()` reads `facts.us-gaap.<tag>.units.USD`, tries `Revenues` then falls back to `RevenueFromContractWithCustomerExcludingAssessedTax` (and `NetIncomeLoss` then `ProfitLoss`), picking whichever candidate tag has the most recent `end` date rather than just the first tag present — needed because e.g. Apple stopped reporting under `Revenues` after 2018 and switched tags, so "first tag that exists" alone returned 2018 data. Verified against Apple, Tesla, Microsoft — all print current-quarter Revenue/NetIncomeLoss with the correct tag and period. Also fixed number formatting (`toLocaleString('en-US')`) after it defaulted to Indian digit grouping.
- [x] Phase 0, step 6 — Added a `response.ok` check before parsing JSON: SEC returns a 404 with an XML error body (not JSON) both for a nonexistent CIK and for a real CIK that has never filed XBRL data (confirmed CIK `1` 404s identically to a made-up CIK) — previously this crashed with an uncaught `SyntaxError` from `JSON.parse`. Now prints a clear message and exits `1` instead. Verified against a made-up CIK and CIK `1`; re-verified Apple still works.
- [x] Phase 0, step 7 — Verified against 5 real companies: Apple, Tesla, Microsoft, Amazon, Alphabet — all return correct entityName, Revenue, and NetIncomeLoss with the right tag/period. Failure case (nonexistent CIK `9999999999`) exits cleanly with code 1. Also manually confirmed via `curl` that SEC returns 403 for a request without a proper `User-Agent`, matching the roadmap's expected failure case. **Phase 0 "Done when" bar met — all ROADMAP.md Phase 0 checkboxes marked done.**
- [x] Phase 1, step 1 — Installed `express`, `pg`, `zod`, `bcrypt`, `jsonwebtoken` as dependencies, plus `@types/express`, `@types/pg`, `@types/bcrypt`, `@types/jsonwebtoken` as devDependencies (zod ships its own types).
- [x] Phase 1, step 2 — PostgreSQL 18.6 installed natively on Windows (service `postgresql-x64-18` running, NOT Docker per CLAUDE.md). Created the `edgar_radar` database. Verified connectivity two ways: `psql` CLI (manual, by the user) and the actual `pg` Node driver via a throwaway `db-check.ts` script reading `DATABASE_URL` from `.env` (removed after verifying — not a roadmap deliverable). Along the way, hit and fixed a real `nodenext`/`verbatimModuleSyntax` issue: `pg` ships both ESM and CJS builds via conditional exports, and TypeScript resolves a plain `import ... from 'pg'` to the ESM build even though our files compile as CommonJS — fixed by using `import pg = require('pg')` instead.
- [x] Phase 1, step 3 — Installed `node-pg-migrate`; added `npm run migrate` script (wraps its CLI, reads `DATABASE_URL` from `.env` automatically). Wrote [migrations/1788959633418_companies-table.js](migrations/1788959633418_companies-table.js): `companies` table with `cik CHAR(10)` as the primary key (natural key per ROADMAP's key decision, not a surrogate id), `entity_name`, `created_at`, `updated_at`. Verified `up` creates the table, `down` drops it cleanly, and re-running `up` restores it — full round-trip tested.
- [x] Phase 1, step 4 — Extracted Phase 0's fetch/parse logic out of `index.ts` into [src/sec.ts](src/sec.ts) (`fetchCompanyFacts`, `mostRecentFact`, `padCik`, tag lists, `CompanyFactsNotFoundError`) so it's reused, not duplicated; `index.ts` now imports from it and still behaves identically (re-verified). Added [src/db.ts](src/db.ts) (pg `Pool` reading `DATABASE_URL`) and [migrations/1788959858606_filing-facts-table.js](migrations/1788959858606_filing-facts-table.js) — a `filing_facts` EAV table (`cik` FK → `companies`, `tag`, `unit`, `value`, `period_end`, `fiscal_year`, `fiscal_period`, `form`, `filed_date`, unique on `(cik, tag, unit, period_end, form)`), per ROADMAP's "long/EAV table, not wide columns" decision. Added [src/repositories/companyRepository.ts](src/repositories/companyRepository.ts) with `upsertCompanyFacts(cik)`, upserting the company row plus its most recent Revenue/NetIncomeLoss facts via `ON CONFLICT ... DO UPDATE`. Verified end-to-end against Apple and Tesla (real SEC data written to real Postgres tables) and confirmed re-running the upsert for the same company doesn't create duplicate rows.
  - Also fixed a real, project-wide tooling issue while wiring this up: `tsconfig.json`'s `verbatimModuleSyntax: true` (a `tsc --init` default) blocks *any* `import { x } from 'y'` with real bindings in our CommonJS-format files — only side-effect imports and the legacy `import x = require('y')` syntax were exempt. This would have broken on every future local-module import (not just the earlier `pg`-specific case). Set `verbatimModuleSyntax: false` to restore normal import syntax project-wide.
- [x] Phase 1, step 5 — Added [src/app.ts](src/app.ts) (Express app) and [src/server.ts](src/server.ts) (entrypoint; `npm run dev`), plus [src/routes/companies.ts](src/routes/companies.ts) with `GET /companies/:cik` and `GET /companies/:cik/facts`. Both use a get-or-fetch pattern via new `getCompanyByCik`/`getFactsByCik` reads in [companyRepository.ts](src/repositories/companyRepository.ts): serve from Postgres if already stored, otherwise call `upsertCompanyFacts` (live SEC fetch + store) once and serve from the DB afterward — so repeat requests don't re-hit SEC. Verified all cases against the running server: Apple (already in DB) served instantly from Postgres; Microsoft (never ingested) triggered a live fetch+store on first request and was served from DB on the follow-up `/facts` call; invalid CIK format (`abc`) → `400`; nonexistent CIK → `404` with the same message as the Phase 0 script.
- [x] Phase 1, step 6 — Added [migrations/1788978979286_users-table.js](migrations/1788978979286_users-table.js) (`users`: `id`, unique `email`, `password_hash`, timestamps). Added [src/repositories/userRepository.ts](src/repositories/userRepository.ts) (`createUser`, catching Postgres's unique-violation code `23505` and converting it to a `DuplicateEmailError`) and [src/routes/auth.ts](src/routes/auth.ts) with `POST /auth/register`: validates `{ email, password }` with a `zod` schema (`email` must be a valid email, `password` min 8 chars), hashes the password with `bcrypt` (10 salt rounds) before it ever reaches the database, and never returns the hash in the response. Mounted at `/auth` in [src/app.ts](src/app.ts).
  - Tested against the real running server: successful registration → `201` with `{id, email, createdAt}` (no password field); registering the same email again → `409` via `DuplicateEmailError`; invalid email + short password → `400` with per-field zod errors; empty body → `400`.
  - Hit and fixed an environment issue along the way: the server process from step 5's testing was still running and holding port 3000 — `kill %1` in Git Bash on Windows killed the shell's job wrapper but not the actual `node.exe` child process, so requests were silently hitting the stale pre-auth build (`404 Cannot POST /auth/register`) instead of the current code. Diagnosed via `netstat -ano` + `tasklist`, force-killed the real PID with `taskkill //F //PID`, confirmed a fresh process held the port, then re-tested successfully. Worth remembering for future steps: always verify the actual listening PID after restarting the dev server on this machine, don't assume `kill %1` was enough.
- [x] Phase 1, step 7 — Added `JWT_SECRET` (random 96-hex-char value) to `.env`/`.env.example`. Added `getUserByEmail()` to [userRepository.ts](src/repositories/userRepository.ts). Added `POST /auth/login` to [auth.ts](src/routes/auth.ts): validates `{ email, password }` via zod, looks up the user, compares the password with `bcrypt.compare`, and on success signs a JWT (`jsonwebtoken`, payload `{ sub, email }`, 1h expiry) with `JWT_SECRET`. On failure (unknown email or wrong password) both return an identical generic `401 "Invalid email or password."` — and `bcrypt.compare` always runs (against a precomputed dummy hash when the email doesn't exist) so an unknown-email response takes the same time as a wrong-password response, closing a timing side-channel that could otherwise be used to enumerate registered emails.
  - Tested against the real server: correct credentials → `200` with a JWT; decoded the token to confirm the payload (`sub`, `email`, `iat`, `exp` = +1h); wrong password → `401`; unknown email → `401` (same message); invalid body → `400`.
- [x] Phase 1, step 8 — Added [src/middleware/requireAuth.ts](src/middleware/requireAuth.ts): reads the `Authorization: Bearer <token>` header, verifies it with `jsonwebtoken` against `JWT_SECRET`, attaches `{ id, email }` to `req.user` on success, and returns `401` on a missing/malformed header or an invalid/expired/tampered token. Added [src/types/express.d.ts](src/types/express.d.ts) to type-augment Express's `Request` with `user`. Not mounted on a real route yet — that's step 9 (watchlist endpoints) — so tested it directly with a throwaway script (mock req/res/next, since removed) covering: valid token, missing header, malformed header, expired token, tampered token, and a token signed with the wrong secret. All 9 checks passed, including the roadmap's explicit "expired/tampered JWT" case.
  - Hit and fixed a real ts-node config gap: by default `ts-node` only type-checks files reachable from the entrypoint's import graph, not everything `tsconfig.json` would include — so a pure ambient `.d.ts` global augmentation (like `express.d.ts`, which has no runtime output and can't be `require()`'d) was silently invisible to `req.user` typing depending on which file was run, even though whole-project `tsc --noEmit` saw it fine. Fixed properly via ts-node's documented `"ts-node": { "files": true }` option in `tsconfig.json`, rather than a fragile per-file workaround. Re-verified `index.ts` still runs correctly after the config change.
- [x] Phase 1, step 9 — Added [migrations/1788982592507_watchlists-table.js](migrations/1788982592507_watchlists-table.js): `watchlists` (`user_id` FK → `users`, `cik` FK → `companies`, unique on `(user_id, cik)` so the same company can't be watchlisted twice). Added [src/repositories/watchlistRepository.ts](src/repositories/watchlistRepository.ts) (`addToWatchlist` reuses the get-or-fetch pattern — auto-ingests the company from SEC if it isn't already stored — and converts a unique-violation into `DuplicateWatchlistEntryError`; `removeFromWatchlist`; `getWatchlist`). Added [src/routes/watchlist.ts](src/routes/watchlist.ts) with `GET/POST /watchlist` and `DELETE /watchlist/:cik`, all behind `requireAuth` (mounted via `router.use(requireAuth)`). Mounted at `/watchlist` in `app.ts`.
  - Ran the full Phase 1 "Done when" flow end-to-end against the real server: register → login → watchlist Apple, Tesla, and Amazon (Amazon had never been touched before — confirmed the auto-ingest fired) → fetched facts for all 3 via the existing `/companies/:cik/facts` endpoint. Also verified: no token → `401`; duplicate watchlist add → `409`; invalid CIK format in body → `400`; `DELETE` an entry → `204`, then `DELETE` again → `404`; `GET /watchlist` reflects the removal.
- [x] Phase 1, step 10 — Audited every route for body-accepting endpoints (`grep` across `src/routes` for `req.body`/`router.post|put|patch`): only three exist — `POST /auth/register`, `POST /auth/login`, `POST /watchlist` — and all three already went through a `zod` schema's `.safeParse()` as they were built (no gaps found, no code changes needed). `GET`/`DELETE` routes correctly have no body validation since they don't accept one.
  - While here, also closed out the one remaining untested Phase 1 failure case from ROADMAP.md: **SQL-injection resistance**. Wrote a throwaway script calling `getCompanyByCik()` directly (bypassing the route's regex validation on purpose) with the classic payload `0000320193' OR '1'='1` — confirmed it returned `null` (treated as an inert literal by the parameterized query) rather than leaking all rows, which is what would happen if any query used string concatenation instead of `$1`-style placeholders. Script removed after verifying.
  - **Phase 1 "Done when" bar fully met — all ROADMAP.md Phase 1 checkboxes (Build + Test failure cases) now checked.**

## Phase 1 summary
Built the real Express + Postgres API described in ROADMAP.md, on top of Phase 0's throwaway script:
- **Data layer:** `companies` and `filing_facts` (EAV, not wide columns, per the roadmap's key decision) tables via `node-pg-migrate`, plus `users` and `watchlists`. Phase 0's SEC fetch/parse logic was extracted into `src/sec.ts` and reused (not duplicated) by a `companyRepository` that upserts a company + its facts idempotently.
- **API:** `GET /companies/:cik` and `/companies/:cik/facts` (get-or-fetch: serve from Postgres, auto-ingest from SEC on first request for a company); `POST /auth/register` and `POST /auth/login` (bcrypt hashing, JWT issuance, timing-safe login); `requireAuth` JWT middleware; `GET/POST/DELETE /watchlist` behind that middleware, reusing the same get-or-fetch ingestion path.
- **Validation & security:** every body-accepting endpoint validated with `zod`; all queries parameterized (verified against real SQL-injection payloads); passwords never stored or returned in plaintext; duplicate-safe upserts and unique constraints throughout (companies, users, watchlist entries).
- **Verified against real data and the real server** throughout, not just type-checked — including the full register → login → watchlist 3 companies → fetch facts flow, and all 4 of ROADMAP's Phase 1 failure cases (duplicate email, expired/tampered JWT, SQL injection, duplicate watchlist entry).
- **Along the way**, fixed several real tooling issues rather than working around them: TypeScript 7 vs. `ts-node` incompatibility, `pg`'s dual ESM/CJS exports under `nodenext`, a project-wide `verbatimModuleSyntax` misconfiguration, `ts-node`'s default file-inclusion behavior missing ambient `.d.ts` files, and a stale background dev-server process silently serving old code during step 6 testing.
- [ ] Phase 0, step 5 — Parse + print most recent Revenues / NetIncomeLoss, with tag fallback
- [ ] Phase 0, step 6 — Handle bad-CIK and no-XBRL-data cases without crashing
- [ ] Phase 0, step 7 — Verified against 5 real companies including a failure case
