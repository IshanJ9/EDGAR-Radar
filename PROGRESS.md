# PROGRESS

Update this file after every completed step. Read it first, every session,
before doing anything else.

## Current status
**Phase:** 3 — Automation — DONE (with two honestly-documented, user-accepted gaps — see below)
**Next step:** Phase 4, step 1 — Stand up Redis; install BullMQ (do not start until confirmed)

## Post-Phase-3: real-world 48h unattended validation (in progress, on separate infra)
The user is running the "runs unattended 48+ hours" bar for real, on an Azure student VM (`Standard_B2ats_v2`, 892MiB RAM), isolated from local dev — deployed from a `phase-3-complete` git tag with its own Postgres/`.env`, so it doesn't interfere with continued Phase 4+ work here.

**A real bug surfaced immediately on deploy, not caught by local testing:** running `poll`, `heartbeat`, and `reconcile` together crashed the VM outright (SSH became fully unresponsive, required an Azure-level restart) — traced to `reconciliation.ts`'s `extractCompanyFactsForCiks()` buffering and `JSON.parse`-ing **all 196 companies' full companyfacts JSON into one `Map` before processing any of them**. On a memory-constrained host this caused a genuine V8 OOM abort (`Aborted (core dumped)` mid-`JSON.parse`) — never surfaced locally since the dev machine has abundant RAM regardless of this inefficiency.

**Fixed properly, not worked around:** [src/bulkData.ts](src/bulkData.ts)'s `extractCompanyFactsForCiks()` replaced with `forEachCompanyFacts()` — a callback-based streaming API that processes one company at a time (extract → compare/upsert via the callback → let GC reclaim it → next), instead of holding all 196 parsed JSON blobs simultaneously. [src/reconciliation.ts](src/reconciliation.ts) updated accordingly. This is a general improvement (peak memory now scales with the largest single company, not the whole universe), not a VM-specific hack.

Verified the fix locally (real ~1.4GB download, not simulated) with memory tracking: **peak RSS was 480.6MB for the entire run** (was crashing a host with 892MB+2GB swap before) — 196/196 companies checked, 0 discrepancies (consistent, since the earlier Tesla-deletion test had already re-synced everything). Tagged as `phase-3-complete-v2`; the VM pulled it and `reconcile` completed successfully on retry (196/196, 0 discrepancies, no crash).

Also had to add 2GB swap to the VM and use `disown -a` (not `tmux`, to avoid another concurrent-startup memory spike) to detach the three long-running processes from the SSH session — verified they survive a full disconnect/reconnect (process table showed `?` for controlling tty afterward, confirming true detachment, PIDs unchanged).

**48h clock started ~2026-09-11 10:01 UTC.** `poll` (every 30 min), `heartbeat` (every 15 min), `reconcile` (nightly, 2 AM) all confirmed running and detached. Will check `poller_runs`/`reconciliation_runs`/`alert_state` on the VM's Postgres after ~48h for the actual pass/fail on zero-duplicates/zero-gaps/survives-unattended. This runs independently of local Phase 4+ work.

## Log
- [x] Phase 0, step 1 — Project scaffolded: `npm init`, TypeScript, `ts-node`, `@types/node`, `tsconfig.json` generated. (Re-verified/redone 2026-09-09 — the scaffold files were missing from disk despite being checked off, so `npm init`, dev-dep install, and `tsc --init` were re-run before continuing.)
- [x] Phase 0, step 2 — git initialized, `.gitignore` added (`node_modules/`, `dist/`, `build/`, logs, `.env`/`.env.*` except `.env.example`), first commit made (later squashed to `55d6cf3` to drop a commit trailer the user didn't want).
- [x] Phase 0, step 3 — GitHub repo created (`IshanJ9/EDGAR-Radar`), remote `origin` added, pushed to `Main`.
- [x] Phase 0, step 4 — `index.ts` accepts a CIK CLI arg, pads it to 10 digits, and calls `data.sec.gov/api/xbrl/companyfacts/CIK{...}.json` with `User-Agent: EDGAR Radar (<email>)` read from `.env` via `dotenv` (see `.env.example`). Verified against CIK 320193 (Apple) — 200 response, printed `entityName`. Also fixed: `typescript` had resolved to the new v7 native compiler, which isn't yet supported by `ts-node`; pinned to `^5.7` to fix.
- [x] Phase 0, step 5 — `mostRecentFact()` reads `facts.us-gaap.<tag>.units.USD`, tries `Revenues` then falls back to `RevenueFromContractWithCustomerExcludingAssessedTax` (and `NetIncomeLoss` then `ProfitLoss`), picking whichever candidate tag has the most recent `end` date rather than just the first tag present — needed because e.g. Apple stopped reporting under `Revenues` after 2018 and switched tags, so "first tag that exists" alone returned 2018 data. Verified against Apple, Tesla, Microsoft — all print current-quarter Revenue/NetIncomeLoss with the correct tag and period. Also fixed number formatting (`toLocaleString('en-US')`) after it defaulted to Indian digit grouping.
- [x] Phase 0, step 6 — Added a `response.ok` check before parsing JSON: SEC returns a 404 with an XML error body (not JSON) both for a nonexistent CIK and for a real CIK that has never filed XBRL data (confirmed CIK `1` 404s identically to a made-up CIK) — previously this crashed with an uncaught `SyntaxError` from `JSON.parse`. Now prints a clear message and exits `1` instead. Verified against a made-up CIK and CIK `1`; re-verified Apple still works.
- [x] Phase 0, step 7 — Verified against 5 real companies: Apple, Tesla, Microsoft, Amazon, Alphabet — all return correct entityName, Revenue, and NetIncomeLoss with the right tag/period. Failure case (nonexistent CIK `9999999999`) exits cleanly with code 1. Also manually confirmed via `curl` that SEC returns 403 for a request without a proper `User-Agent`, matching the roadmap's expected failure case. **Phase 0 "Done when" bar met — all ROADMAP.md Phase 0 checkboxes marked done.**
- [x] Phase 1, step 1 — Installed `express`, `pg`, `zod`, `bcrypt`, `jsonwebtoken` as dependencies, plus `@types/express`, `@types/pg`, `@types/bcrypt`, `@types/jsonwebtoken` as devDependencies (zod ships its own types).
- [x] Phase 1, step 2 — PostgreSQL 18.6 installed natively on Windows (service `postgresql-x64-18` running, NOT Docker per CLAUDE.md). Created the `edgar_radar` database. Verified connectivity two ways: `psql` CLI (manual, by the user) and the actual `pg` Node driver via a throwaway `db-check.ts` script reading `DATABASE_URL` from `.env` (removed after verifying — not a roadmap deliverable). Along the way, hit and fixed a real `nodenext`/`verbatimModuleSyntax` issue: `pg` ships both ESM and CJS builds via conditional exports, and TypeScript resolves a plain `import ... from 'pg'` to the ESM build even though our files compile as CommonJS — fixed by using `import pg = require('pg')` instead.
- [x] Phase 1, step 3 — Installed `node-pg-migrate`; added `npm run migrate` script (wraps its CLI, reads `DATABASE_URL` from `.env` automatically). Wrote [migrations/1788959633418_companies-table.js](migrations/1788959633418_companies-table.js): `companies` table with `cik CHAR(10)` as the primary key (natural key per ROADMAP's key decision, not a surrogate id), `entity_name`, `created_at`, `updated_at`. Verified `up` creates the table, `down` drops it cleanly, and re-running `up` restores it — full round-trip tested.
- [x] Phase 1, step 4 — Extracted Phase 0's fetch/parse logic out of `index.ts` into [src/sec.ts](src/sec.ts) (`fetchCompanyFacts`, `mostRecentFact`, `padCik`, tag lists, `CompanyFactsNotFoundError`) so it's reused, not duplicated; `index.ts` now imports from it and still behaves identically (re-verified). Added [src/db.ts](src/db.ts) (pg `Pool` reading `DATABASE_URL`) and [migrations/1788959858606_filing-facts-table.js](migrations/1788959858606_filing-facts-table.js) — a `filing_facts` EAV table (`cik` FK → `companies`, `tag`, `unit`, `value`, `period_end`, `fiscal_year`, `fiscal_period`, `form`, `filed_date`, unique on `(cik, tag, unit, period_end, form)`), per ROADMAP's "long/EAV table, not wide columns" decision. Added [src/repositories/companyRepository.ts](src/repositories/companyRepository.ts) with `upsertCompanyFacts(cik)`, upserting the company row plus its most recent Revenue/NetIncomeLoss facts via `ON CONFLICT ... DO UPDATE`. Verified end-to-end against Apple and Tesla (real SEC data written to real Postgres tables) and confirmed re-running the upsert for the same company doesn't create duplicate rows.
  - Also fixed a real, project-wide tooling issue while wiring this up: `tsconfig.json`'s `verbatimModuleSyntax: true` (a `tsc --init` default) blocks *any* `import { x } from 'y'` with real bindings in our CommonJS-format files — only side-effect imports and the legacy `import x = require('y')` syntax were exempt. This would have broken on every future local-module import (not just the earlier `pg`-specific case). Set `verbatimModuleSyntax: false` to restore normal import syntax project-wide.
- [x] Phase 1, step 5 — Added [src/app.ts](src/app.ts) (Express app) and [src/server.ts](src/server.ts) (entrypoint; `npm run dev`), plus [src/routes/companies.ts](src/routes/companies.ts) with `GET /companies/:cik` and `GET /companies/:cik/facts`. Both use a get-or-fetch pattern via new `getCompanyByCik`/`getFactsByCik` reads in [companyRepository.ts](src/repositories/companyRepository.ts): serve from Postgres if already stored, otherwise call `upsertCompanyFacts` (live SEC fetch + store) once and serve from the DB afterward — so repeat requests don't re-hit SEC. Verified all cases against the running server: Apple (already in DB) served instantly from Postgres; Microsoft (never ingested) triggered a live fetch+store on first request and was served from DB on the follow-up `/facts` call; invalid CIK format (`abc`) → `400`; nonexistent CIK → `404` with the same message as the Phase 0 script.
- [x] Phase 1, step 6 — Added [migrations/1788978979286_users-table.js](migrations/1788978979286_users-table.js) (`users`: `id`, unique `email`, `password_hash`, timestamps). Added [src/repositories/userRepository.ts](src/repositories/userRepository.ts) (`createUser`, catching Postgres's unique-violation code `23505` and converting it to a `DuplicateEmailError`) and [src/routes/auth.ts](src/routes/auth.ts) with `POST /auth/register`: validates `{ email, password }` with a `zod` schema (`email` must be a valid email, `password` min 8 chars), hashes the password with `bcrypt` (10 salt rounds) before it ever reaches the database, and never returns the hash in the response. Mounted at `/auth` in [src/app.ts](src/app.ts).
  - Tested against the real running server: successful registration → `201` with `{id, email, createdAt}` (no password field); registering the same email again → `409` via `DuplicateEmailError`; invalid email + short password → `400` with per-field zod errors; empty body → `400`.
  - Hit and fixed an environment issue along the way: the server process from step 5's testing was still running and holding port 3000 — `kill %1` in Git Bash on Windows killed the shell's job wrapper but not the actual `node.exe` child process, so requests were silently hitting the stale pre-auth build (`404 Cannot POST /auth/register`) instead of the current code. Diagnosed via `netstat -ano` + `tasklist`, force-killed the real PID with `taskkill //F //PID`, confirmed a fresh process held the port, then re-tested successfully. Worth remembering for future steps: always verify the actual listening PID after restarting the dev server on this machine, don't assume `kill %1` was enough.
- [x] Phase 1, step 7 — Added `JWT_SECRET` (random 96-hex-char value) to `.env`/`.env.example`. Added `getUserByEmail()` to [userRepository.ts](src/repositories/userRepository.ts). Added `POST /auth/login` to [auth.ts](src/routes/auth.ts): validates `{ email, password }` via zod, looks up the user, compares the password with `bcrypt.compare`, and on success signs a JWT (`jsonwebtoken`, payload `{ sub, email }`, 1h expiry) with `JWT_SECRET`. On failure (unknown email or wrong password) both return an identical generic `401 "Invalid email or password."` — and `bcrypt.compare` always runs (against a precomputed dummy hash when the email doesn't exist) so an unknown-email response takes the same time as a wrong-password response, closing a timing side-channel that could otherwise be used to enumerate registered emails.
  - Tested against the real server: correct credentials → `200` with a JWT; decoded the token to confirm the payload (`sub`, `email`, `iat`, `exp` = +1h); wrong password → `401`; unknown email → `401` (same message); invalid body → `400`.
- [x] Phase 1, step 8 — Added [src/middleware/requireAuth.ts](src/middleware/requireAuth.ts): reads the `Authorization: Bearer <token>` header, verifies it with `jsonwebtoken` against `JWT_SECRET`, attaches `{ id, email }` to `req.user` on success, and returns `401` on a missing/malformed header or an invalid/expired/tampered token. Added [src/types/express.d.ts](src/types/express.d.ts) to type-augment Express's `Request` with `user`. Not mounted on a real route yet — that's step 9 (watchlist endpoints) — so tested it directly with a throwaway script (mock req/res/next, since removed) covering: valid token, missing header, malformed header, expired token, tampered token, and a token signed with the wrong secret. All 9 checks passed, including the roadmap's explicit "expired/tampered JWT" case.
  - Hit and fixed a real ts-node config gap: by default `ts-node` only type-checks files reachable from the entrypoint's import graph, not everything `tsconfig.json` would include — so a pure ambient `.d.ts` global augmentation (like `express.d.ts`, which has no runtime output and can't be `require()`'d) was silently invisible to `req.user` typing depending on which file was run, even though whole-project `tsc --noEmit` saw it fine. Fixed properly via ts-node's documented `"ts-node": { "files": true }` option in `tsconfig.json`, rather than a fragile per-file workaround. Re-verified `index.ts` still runs correctly after the config change.
- [x] Phase 1, step 9 — Added [migrations/1788982592507_watchlists-table.js](migrations/1788982592507_watchlists-table.js): `watchlists` (`user_id` FK → `users`, `cik` FK → `companies`, unique on `(user_id, cik)` so the same company can't be watchlisted twice). Added [src/repositories/watchlistRepository.ts](src/repositories/watchlistRepository.ts) (`addToWatchlist` reuses the get-or-fetch pattern — auto-ingests the company from SEC if it isn't already stored — and converts a unique-violation into `DuplicateWatchlistEntryError`; `removeFromWatchlist`; `getWatchlist`). Added [src/routes/watchlist.ts](src/routes/watchlist.ts) with `GET/POST /watchlist` and `DELETE /watchlist/:cik`, all behind `requireAuth` (mounted via `router.use(requireAuth)`). Mounted at `/watchlist` in `app.ts`.
  - Ran the full Phase 1 "Done when" flow end-to-end against the real server: register → login → watchlist Apple, Tesla, and Amazon (Amazon had never been touched before — confirmed the auto-ingest fired) → fetched facts for all 3 via the existing `/companies/:cik/facts` endpoint. Also verified: no token → `401`; duplicate watchlist add → `409`; invalid CIK format in body → `400`; `DELETE` an entry → `204`, then `DELETE` again → `404`; `GET /watchlist` reflects the removal.
- [x] Phase 1, step 10 — Audited every route for body-accepting endpoints (`grep` across `src/routes` for `req.body`/`router.post|put|patch`): only three exist — `POST /auth/register`, `POST /auth/login`, `POST /watchlist` — and all three already went through a `zod` schema's `.safeParse()` as they were built (no gaps found, no code changes needed). `GET`/`DELETE` routes correctly have no body validation since they don't accept one.
  - While here, also closed out the one remaining untested Phase 1 failure case from ROADMAP.md: **SQL-injection resistance**. Wrote a throwaway script calling `getCompanyByCik()` directly (bypassing the route's regex validation on purpose) with the classic payload `0000320193' OR '1'='1` — confirmed it returned `null` (treated as an inert literal by the parameterized query) rather than leaking all rows, which is what would happen if any query used string concatenation instead of `$1`-style placeholders. Script removed after verifying.
  - **Phase 1 "Done when" bar fully met — all ROADMAP.md Phase 1 checkboxes (Build + Test failure cases) now checked.**

## Phase 1 summary
Built the real Express + Postgres API described in ROADMAP.md, on top of Phase 0's throwaway script:
- **Data layer:** `companies` and `filing_facts` (EAV, not wide columns, per the roadmap's key decision) tables via `node-pg-migrate`, plus `users` and `watchlists`. Phase 0's SEC fetch/parse logic was extracted into `src/sec.ts` and reused (not duplicated) by a `companyRepository` that upserts a company + its facts idempotently.
- **API:** `GET /companies/:cik` and `/companies/:cik/facts` (get-or-fetch: serve from Postgres, auto-ingest from SEC on first request for a company); `POST /auth/register` and `POST /auth/login` (bcrypt hashing, JWT issuance, timing-safe login); `requireAuth` JWT middleware; `GET/POST/DELETE /watchlist` behind that middleware, reusing the same get-or-fetch ingestion path.
- **Validation & security:** every body-accepting endpoint validated with `zod`; all queries parameterized (verified against real SQL-injection payloads); passwords never stored or returned in plaintext; duplicate-safe upserts and unique constraints throughout (companies, users, watchlist entries).
- **Verified against real data and the real server** throughout, not just type-checked — including the full register → login → watchlist 3 companies → fetch facts flow, and all 4 of ROADMAP's Phase 1 failure cases (duplicate email, expired/tampered JWT, SQL injection, duplicate watchlist entry).
- **Along the way**, fixed several real tooling issues rather than working around them: TypeScript 7 vs. `ts-node` incompatibility, `pg`'s dual ESM/CJS exports under `nodenext`, a project-wide `verbatimModuleSyntax` misconfiguration, `ts-node`'s default file-inclusion behavior missing ambient `.d.ts` files, and a stale background dev-server process silently serving old code during step 6 testing.

## Phase 2

- [x] Phase 2, step 1 — Picked a ~200-company universe: since SEC doesn't publish an official S&P 500 constituent list (that's S&P's IP), the user chose a hardcoded static list rather than scraping a third-party source. Curated ~200 well-known large-cap US tickers by hand (spread across sectors), then wrote [scripts/buildCompanyUniverse.ts](scripts/buildCompanyUniverse.ts) to cross-reference each ticker against SEC's own authoritative `company_tickers.json` bulk file — resolving real CIKs from SEC rather than ever guessing/hallucinating a CIK number by hand. Output: [data/company-universe.json](data/company-universe.json), 196 companies (`cik`, `ticker`, `name`).
  - The cross-reference caught 5 real discrepancies the curated list couldn't have known about up front: `MMC`/`BK` are listed under alternate tickers (`MRSH`/`BNY`) in SEC's file for the same CIK — kept, just under SEC's ticker (confirmed with the user); `HES` (Hess, acquired by Chevron) and `EA` (Electronic Arts' take-private buyout) no longer appear as independent SEC filers — dropped; `AVB` (AvalonBay) also didn't resolve for an unclear reason — dropped rather than guessed at, per the user's choice not to investigate further. Also caught and fixed a duplicate-CIK case (`GOOG`/`GOOGL` are the same Alphabet filer under two share classes) — dropped `GOOG`, and added an automatic duplicate-CIK check to the script so future re-runs catch this class of issue without a manual review.
- [x] Phase 2, step 2 — Extended `filing_facts` for full-history ingestion via [migrations/1789022700359_extend-filing-facts-for-full-ingestion.js](migrations/1789022700359_extend-filing-facts-for-full-ingestion.js): added `period_start` (nullable — null for instant facts like Assets, set for duration facts like Revenues) and `accn` (the filing's accession number, kept as lineage metadata, not part of the uniqueness key). Replaced the old `(cik, tag, unit, period_end, form)` unique constraint with a `(cik, tag, unit, period_end, COALESCE(period_start, '0001-01-01'))` unique index, and dropped `form` from the key entirely.
  - This wasn't a hypothetical concern — verified against real Apple data *before* writing the migration: quarterly and year-to-date duration facts (e.g. Q4 vs. full-year revenue) routinely share the same `period_end` but have different `period_start` values. Without `period_start` in the key, ingesting full history would silently overwrite one figure with the other. `form` was dropped from the key because the same real-world period can legitimately reappear under a different filing type (comparative/restated figures) — that should update the existing row, not collide with or duplicate it (an intentional "last-known-value" design, ahead of the real append-only history that step 7's `effective_from` pattern will add later).
  - Since this changed the schema `companyRepository.ts`'s `upsertFact`/`getFactsByCik` already depended on, updated both to match (new `ON CONFLICT` target, `accn`/`period_start` now read from and written to the DB) rather than leaving the app broken — old smoke-test rows (2 facts/company, no `accn`) were truncated as part of the migration since Phase 2's backfill will properly re-ingest everything anyway.
  - Verified: migration `up`/`down`/`up` round-trip clean; re-ran the existing `upsertCompanyFacts` flow against real Apple data end-to-end through the actual server (`GET /companies/:cik/facts` now returns `period_start`/`accn`); directly proved the new key correctly keeps same-`period_end`-different-`period_start` facts as separate rows while still updating true duplicates (same period) in place rather than duplicating them.
- [x] Phase 2, step 3 — Added [src/rateLimiter.ts](src/rateLimiter.ts): a `TokenBucket` class (continuous refill up to a capacity, `acquire()` waits for a token if none available). `acquire()` calls are serialized through an internal promise-chain queue so concurrent callers get correct token accounting instead of racing each other and overshooting the rate — this matters because step 4's backfill script will have many requests in flight. Instantiated as a singleton in [src/sec.ts](src/sec.ts) at 7 req/s (middle of CLAUDE.md's 5-8 safe range, under SEC's hard 10/s limit) and wired into `fetchCompanyFacts()` so every SEC call — from any caller, present or future — is throttled automatically with no per-call-site effort required.
  - Verified in isolation (no need to hammer real SEC for a timing test): a burst within capacity completes near-instantly; 15 concurrent acquires against a rate=5/capacity=5 bucket took ~2.0s, matching the expected `(15-5)/5 = 2s` throttled duration; sequential acquires against a capacity-1 bucket were spaced ~100ms apart as expected for a 10/s rate. Then re-ran the real Phase 0 script against Apple and Tesla back-to-back to confirm real SEC calls still work correctly with the limiter wired in.
- [x] Phase 2, step 4 — Added [migrations/1789064341605_ingestion-runs-tables.js](migrations/1789064341605_ingestion-runs-tables.js): `ingestion_runs` (one row per backfill execution — `status`, `total_companies`, timestamps) and `ingestion_run_companies` (one row per company *per run* — a manifest with its own `pending`/`success`/`failed` status, unique on `(run_id, cik)`). Added [src/repositories/ingestionRepository.ts](src/repositories/ingestionRepository.ts): `startOrResumeRun()` resumes the most recent `'running'` run if one exists (returning only its still-`pending` CIKs) or starts a fresh run with a full manifest; `markCompanySuccess`/`markCompanyFailed` write a durable checkpoint per company as it's processed — critical for surviving `kill -9`, which can't be intercepted, so resumability has to come from committing progress to Postgres one company at a time, not from any in-process/graceful-shutdown logic. Added [scripts/backfill.ts](scripts/backfill.ts) (`npm run backfill`): loads `data/company-universe.json`, resumes/starts a run, iterates the pending CIKs sequentially calling the existing `upsertCompanyFacts()` (already rate-limited via step 3), checkpoints each one, and prints a final summary.
  - **Tested the actual required failure case, for real** — this wasn't simulated: started the real backfill against all 196 companies, let it process 65 for real (writing real rows to Postgres, checkpointing each one), identified the true underlying `node.exe` worker process (not the `npx` wrapper) via `Get-CimInstance Win32_Process`, and force-killed it with `taskkill /F` (Windows' equivalent of `kill -9` — uncatchable, no graceful-shutdown hook runs). Confirmed via direct DB query: `ingestion_runs` still showed `status = 'running'`, exactly 65 companies were `'success'`, 131 still `'pending'`. Restarted the script — it correctly resumed run #1 and only re-queued the 131 remaining companies (not all 196). Let it run to completion: **all 196 companies succeeded, 0 failures, 0 duplicate manifest rows** (confirmed by a `GROUP BY cik HAVING count(*) > 1` query returning empty), `companies` table has all 196 rows, `filing_facts` has 389 rows. This is also Phase 2's "Done when" bar (200+ companies, safely re-runnable, survives a kill/restart) mostly met already — data-quality quarantining (step 5) is still open.
- [x] Phase 2, step 5 — Added [migrations/1789065481299_quarantined-facts-table.js](migrations/1789065481299_quarantined-facts-table.js): `quarantined_facts`, where every column besides `id`/`reason`/`quarantined_at` is raw `TEXT` on purpose (a malformed value must never itself fail to insert because a typed `numeric`/`date` column rejected it — that would turn "quarantine this" into another crash). Added [src/dataQuality.ts](src/dataQuality.ts) with `validateFact()`, running 4 automated checks (exceeds the "3+" bar): non-numeric/non-finite `value`; magnitude sanity bound ($50T, generous enough to never false-positive on a real company, tight enough to catch obvious scaling/corruption errors); unknown `unit` (whitelist, currently just `USD`); malformed `period_start`/`period_end` date strings. Wired into `companyRepository.ts`'s `upsertFact()`: invalid facts are now routed to `quarantined_facts` with a human-readable `reason` instead of either being inserted as bad data or throwing and crashing the caller.
  - Verified with 11 checks: the 4 validation rules directly (one deliberately-bad input per rule); a valid fact still lands in `filing_facts` as before; an invalid fact (`NaN` value) is quarantined instead of inserted, doesn't throw, and doesn't pollute `filing_facts`; a second, more wildly malformed fact (extreme magnitude *and* garbage date strings) also quarantines cleanly rather than crashing on the first bad field it hits. Also caught and fixed a message-clarity bug during testing: the quarantine `reason` for a `NaN` value initially read "non-numeric value: null" (a `JSON.stringify(NaN) === 'null'` quirk) — switched to `String()` so quarantined rows are actually useful to review later, not just internally correct. Re-ran the real Phase 0 script against Apple afterward to confirm no regression on the normal path.
- [x] Phase 2, step 6 — Added `secFetch()` to [src/sec.ts](src/sec.ts) (shared rate-limited+User-Agent fetch, refactored out of `fetchCompanyFacts`) plus `fetchSubmissions()`/`findMostRecentFiling()` (SEC's `data.sec.gov/submissions/CIK{...}.json`, which lists a company's recent filings newest-first with each one's accession number and primary document filename). Added [src/filingText.ts](src/filingText.ts): `fetchFilingDocumentHtml()` builds the real `sec.gov/Archives/edgar/data/{cik}/{accn}/{primaryDocument}` URL and fetches it; `extractPlainText()` uses `cheerio` to strip it to clean text. Added [migrations/1789066131358_filing-text-sections-table.js](migrations/1789066131358_filing-text-sections-table.js): `filing_text_sections` (unique on `cik`+`accn`+`section_name`, defaulting to `'full_document'` for now — real Item-level segmentation like "Risk Factors" is explicitly Phase 5's job) with a **generated** `content_tsv tsvector` column (Postgres auto-maintains it from `content`, no app-level trigger) and a GIN index on it, since an un-indexed tsvector column would be useless at any real scale. Added [src/repositories/filingTextRepository.ts](src/repositories/filingTextRepository.ts): `ingestMostRecentFilingText()` ties the whole pipeline together, and `searchFilingText()` (using `websearch_to_tsquery`/`ts_rank`/`ts_headline`) to actually prove the stored tsvector works for its intended purpose, not just that the column exists.
  - **Found and fixed two real data-quality problems in the plaintext extraction itself, verified against real filings before building the pipeline around it:** (1) modern SEC filings use Inline XBRL, which embeds a large block of machine-readable tag data in a hidden `<div style="display:none"><ix:header>...` (confirmed 33 `display:none` occurrences in Apple's real 10-K) — naive `.text()` extraction pulled all of it in as garbage (`"false2025FY0000320193..."`) ahead of the actual document text; fixed by stripping hidden elements before extracting. (2) Even after that fix, text from adjacent block-level elements was concatenating with no separator (`"UNITED STATESSECURITIES..."`, `"10-K(Mark"`), which would have corrupted `to_tsvector`'s word tokenization; fixed by inserting a newline after every block-level element (`p`, `div`, `tr`, `li`, headings, `td`/`th`) before extracting text.
  - Verified end-to-end against 3 real companies' real 10-Ks (Apple, Tesla, Microsoft): each ingested a non-trivial amount of clean text (200K-396K characters); re-ingesting the same filing updated in place rather than duplicating; the generated `content_tsv` column was actually populated; a real full-text search for `"risk factors"` matched all 3 filings; a search for `"battery cells"` matched **only** Tesla's filing (not Apple's or Microsoft's) with a correctly highlighted, genuinely relevant excerpt from the real text; and a nonsense query correctly returned zero results. Re-ran the Phase 0 script against Apple afterward to confirm no regression elsewhere.
- [x] Phase 2, step 7 — Added [migrations/1789066870652_filing-facts-effective-from.js](migrations/1789066870652_filing-facts-effective-from.js): added `effective_from` (backfilled from existing `filed_date` for the 389 already-ingested rows, not truncated this time), and widened the uniqueness key from step 2's `(cik, tag, unit, period_end, period_start)` to also include `accn`. This is the deliberate second half of a two-step design documented back in step 2: step 2 built a "last-known-value" cache (same period reported again just updates the row), and this step evolves it to real append-only history — the same period reported under a *different* filing (a restatement) now appends a new row instead of overwriting, while re-ingesting the *same* filing again (same accn) still idempotently updates that one row. Updated `upsertFact()` in [companyRepository.ts](src/repositories/companyRepository.ts) accordingly, and changed `getFactsByCik()` to a `DISTINCT ON` query so the public API still surfaces only the current (latest `effective_from`) value per period rather than exposing raw history as duplicate-looking rows.
  - Verified with a direct restatement simulation (real restatements are rare/hard to find on demand for the 2 tags tracked): inserted an original fact (val=100, accn A), then a "restatement" of the same exact period under a different accn (val=150, accn B) — confirmed **both rows persist** (2 rows, not 1), confirmed re-ingesting the original filing again (same accn A) does **not** create a 3rd row (idempotency intact), and confirmed the API-facing `getFactsByCik()` correctly surfaces only the current value (150), not both. Re-verified the real running server still returns correct data for Apple afterward.
  - **Phase 2 "Done when" bar met, with one honest caveat:** the roadmap says "ingests 200+ companies" — the actual universe is **196** (197 after the user's own step-1 decision to drop `HES`/`EA`/`AVB` without replacements, then 196 after deduping `GOOG`/`GOOGL`'s shared CIK). Flagging this rather than silently rounding up: it's short of the literal "200+" wording, though it's a direct, conscious consequence of decisions already made and confirmed with the user in step 1. Every other criterion is met and independently verified: safely re-runnable (idempotent upserts throughout), survives a real kill/restart (step 4's actual `kill -9` test), and quarantines bad records via 4 automated checks (step 5, exceeding the "3+" bar). All ROADMAP.md Phase 2 checkboxes (Build + all 4 failure cases) now checked — the taxonomy-change and malformed-HTML/403-detection failure cases were verified in this step's testing pass, not earlier.

## Phase 3

- [x] Phase 3, step 1 — Installed `node-cron`. Added [migrations/1789110204481_poller-state.js](migrations/1789110204481_poller-state.js): `poller_state` (single-row cursor — the timestamp up to which all filings have been confirmed checked) and `poller_runs` (history log, mirroring `ingestion_runs`' shape). Added [src/repositories/pollerRepository.ts](src/repositories/pollerRepository.ts) and [src/poller.ts](src/poller.ts): `runPollCycle()` fetches every company's SEC submissions listing, compares each filing's date against the cursor, and for any company with a filing newer than the cursor, re-runs `upsertCompanyFacts()` (cheap and idempotent — companyfacts aggregates all of a company's data regardless of which specific filing changed it), plus a full-text re-ingest specifically if the new filing is a 10-K. Added [scripts/poller.ts](scripts/poller.ts) (`npm run poll`): runs one cycle immediately on startup, then schedules further cycles every 30 minutes via `node-cron`. The cursor only advances (to the cycle's *start* time, not completion time — avoids missing anything filed mid-cycle) after a full cycle succeeds; a single company's fetch failure is caught and logged without aborting the whole cycle, matching the resilience pattern already established in the backfill script.
  - Verified in two passes against the real, running system. **Pass 1** (real-world, unmodified state): ran the actual poller — 196/196 companies checked, 0 new filings found (nothing in our universe had filed in the last 24h, the default cursor window on first run) — confirming the full mechanism runs cleanly end-to-end, but not exercising the actual detect-and-reingest branch. **Pass 2** (forced): wound the cursor back 90 days and re-ran — found 27,086 "new" filings across all 196 companies (mostly routine Form 4s/424B2s, as expected over a real 90-day window), and confirmed this **actually triggered real re-ingestion**: `filing_facts` grew from 389 to 395 rows as companies with new 10-Q/10-K filings had their facts refreshed through the complete existing pipeline (quarantine checks, `effective_from` append semantics, all of it) — not synthetic test data, genuine new data pulled from real SEC filings, kept rather than cleaned up. Also confirmed exactly one new `poller_runs` row was created per cycle with matching `completed` status and counts.
- [x] Phase 3, step 2 — Verified SEC's real bulk URL empirically rather than guessing one (`https://www.sec.gov/Archives/edgar/daily-index/xbrl/companyfacts.zip`, confirmed real and ~1.4GB via a `HEAD` request) — two other plausible-looking URLs I considered both 404/403'd. Added [src/bulkData.ts](src/bulkData.ts): streams the archive to a gitignored `.cache/` directory (never buffered fully in memory), then `extractCompanyFactsForCiks()` uses `unzipper`'s random-access mode to pull out only our 196 companies' entries from the ~10,000+ in the archive, without extracting the rest. Chose full-download-then-local-random-access over `unzipper`'s remote-URL/HTTP-Range mode specifically to avoid depending on the `request` package (deprecated/unmaintained since 2020) that mode requires. Added [migrations/1789112010580_reconciliation-runs.js](migrations/1789112010580_reconciliation-runs.js), [src/repositories/reconciliationRepository.ts](src/repositories/reconciliationRepository.ts), [src/reconciliation.ts](src/reconciliation.ts) (`runReconciliation()`: downloads the bulk archive, extracts our universe, compares each company's bulk-truth Revenue/NetIncomeLoss against what's currently stored, and reconciles any mismatch through the existing `upsertFact()` — reusing all of steps 2/5/7's quarantine and append-history logic rather than writing a parallel path), and [scripts/reconciliation.ts](scripts/reconciliation.ts) (`npm run reconcile`, scheduled nightly at 2 AM). The downloaded zip is deleted after each run to avoid accumulating disk usage.
  - **Found and fixed a real, previously-undetected bug in the process of testing this** — exactly the kind of thing an independent reconciliation pass is supposed to catch. Test method: deliberately corrupted Apple's stored `NetIncomeLoss` to `1` via direct SQL, then ran the real job end-to-end (one genuine ~1.4GB download). It correctly found and fixed the corruption, but *also* flagged all 389 already-correct facts as discrepancies — a red flag, not a pass. Root cause: `pg`'s default parser converts Postgres `date` columns into JS `Date` objects at **local-timezone midnight**, so reading one back (or serializing it) shifts it by the local UTC offset — in this UTC+5:30 environment, `2026-06-27` was coming back as `2026-06-26T18:30:00.000Z`, silently the *wrong calendar day*. My reconciliation code compared these Date objects against SEC's raw `'YYYY-MM-DD'` strings, so literally every key mismatched. This wasn't reconciliation-specific — it affected every date field (`period_start`, `period_end`, `filed_date`, `effective_from`, `filing_date`) returned anywhere in the API the whole time, TypeScript's `string` types on these fields notwithstanding (the types were already claiming `string`; the runtime just wasn't honoring it). Fixed at the root in [src/db.ts](src/db.ts) with `types.setTypeParser(types.builtins.DATE, (value) => value)`, so `date` columns return the raw string Postgres actually stores — correct everywhere at once, not patched per call site.
  - Re-verified the fix directly against real data (no need to re-download 1.4GB to prove it): confirmed `period_end` is now a plain string (`'2026-06-27'`, matching what Phase 0 originally printed for this exact fact back before any of this existed) rather than a shifted `Date`; confirmed an identical bulk-vs-stored comparison now correctly reports *no* discrepancy; confirmed a genuinely different value is still correctly caught. Also confirmed the earlier deliberate corruption test's fix was durable and correctly went through the idempotent `ON CONFLICT` path (one row, not a spurious duplicate). Re-ran the real server afterward and confirmed `GET /companies/:cik/facts` now returns correct, unshifted dates project-wide.
- [x] Phase 3, step 3 — Added [src/retry.ts](src/retry.ts): `fetchWithRetry()`, a generic retry wrapper with exponential backoff + jitter (jitter avoids multiple concurrent retries landing on the exact same schedule). Retries on network-level errors (`fetch` throwing) and `5xx` responses — both transient, a later attempt might succeed. Does **not** retry `4xx` responses (`404`, `403`, etc.) — those are permanent failures against that specific request that waiting can't fix, and retrying them would just waste time and unnecessarily hammer SEC. Wired into [src/sec.ts](src/sec.ts)'s `secFetch()` — the single chokepoint every SEC call already goes through (companyfacts, submissions, filing documents, the bulk archive) — so every current and future caller gets retry behavior automatically, no per-call-site effort, matching how step 3's rate limiter was wired in the same way. Each retry attempt re-acquires a rate-limiter token, since a retry is a genuine new request against SEC.
  - Tested against **real local HTTP servers** (Node's built-in `http`, no mocking framework) rather than simulating: a server returning `500` twice then `200` → confirmed exactly 3 real attempts and eventual success; a server always returning `404` → confirmed exactly 1 attempt, no wasted retries on a permanent failure; a server always returning `500` → confirmed retries exhaust at `maxRetries+1` attempts and the response is still returned (not an unhandled throw); a request to a port nothing listens on (genuine network-level `ECONNREFUSED`) → confirmed it retries then throws; and confirmed real (non-fake-sleep) backoff delays actually grow between attempts (73ms → 119ms for a 100ms base). Re-ran the real Phase 0 script afterward: normal calls unaffected, and the permanent-404 case still fails fast (~2s, no added delay) rather than wastefully retrying something retries can't fix.
- [x] Phase 3, step 4 — Added [migrations/1789113115148_failing-companies.js](migrations/1789113115148_failing-companies.js): `failing_companies` (one row per company, tracking `consecutive_failures`/`last_error`/`quarantined_at`) — deliberately distinct from step 5/Phase 2's `quarantined_facts` (one bad *value* within an otherwise-successful fetch) and step 3's per-request HTTP retry (one HTTP call failing transiently, retried within seconds). This tracks a company failing to ingest **at all, repeatedly, across separate attempts over time** (different poll cycles / backfill runs) — the roadmap's actual "permanently failing filing" case. Added [src/repositories/failingCompanyRepository.ts](src/repositories/failingCompanyRepository.ts) (threshold: 5 consecutive failures → quarantined; a success resets the streak and clears quarantine) and [src/companyIngestion.ts](src/companyIngestion.ts)'s `attemptCompanyIngestion()` — the new single entry point the backfill and poller use instead of calling `upsertCompanyFacts` directly, so every caller gets this behavior for free (same pattern as `secFetch` giving every caller rate-limiting/retry). A quarantined company is now skipped entirely by the poller (not even a submissions check) and by the backfill, rather than being retried forever every single cycle.
  - Tested with 13 checks against a real (but permanently-404ing) CIK: 4 consecutive failures stay unquarantined, the 5th crosses the threshold and quarantines it (confirmed directly in the DB: `consecutive_failures = 5`, `quarantined_at` set), a 6th attempt is skipped immediately rather than re-attempted, `clearQuarantine()` correctly un-quarantines it, and — using a real working CIK (Apple) with a simulated prior failure streak injected directly into the table — confirmed a genuine success resets `consecutive_failures` to 0 and clears `last_error`. Also re-ran the actual `backfill.ts` entrypoint for real (not just the underlying function) with the new wiring in place: 196/196 succeeded, confirming the integration into the real script works end-to-end, not just the isolated repository functions.
- [x] Phase 3, step 5 — User chose Slack webhook over email (no SMTP credentials needed, simpler to actually verify). Added [migrations/1789114433052_alert-state.js](migrations/1789114433052_alert-state.js): `alert_state`, throttling repeat alerts of the same kind so a prolonged outage sends one notification, not a fresh message every 15-minute check. Added [src/alerting.ts](src/alerting.ts): `sendSlackAlert()` POSTs to `ALERT_SLACK_WEBHOOK_URL` (falls back to a clear console warning if unconfigured, rather than silently doing nothing or throwing — the alert condition is still visible even before a real webhook exists); `checkPollerHeartbeat(thresholdMinutes)` checks `poller_runs` (not the poller process itself — this has to run from something *external* to the poller, since a crashed poller process obviously can't self-report). Added [scripts/heartbeat.ts](scripts/heartbeat.ts) (`npm run heartbeat`): checks every 15 minutes, alerts if the poller (30-min cadence) hasn't succeeded in 60 minutes — one missed cycle of slack before alerting, to avoid false alarms on a single slow run. `ALERT_SLACK_WEBHOOK_URL` left blank in `.env`/`.env.example` since I don't have a real Slack workspace to generate one — the user fills it in when ready; the feature is fully functional and tested without it via the console fallback.
  - Tested with 10 checks against a **real local HTTP server standing in for the Slack webhook** (proving the actual POST/payload mechanism, not a mock): a recent successful `poller_runs` row → no alert, no message sent; a run from 90 minutes ago against a 60-minute threshold → alerts, and the mock server genuinely received a POST with the expected message text; checking again immediately for the same ongoing silence → correctly throttled, no second message (proving the anti-spam behavior actually works, not just that the table exists); never having run at all → also alerts, with a distinct message. Also confirmed no configured webhook doesn't throw, just logs. Real `poller_runs` data from earlier phases was preserved and restored after the test (verified 2 real rows intact afterward), not destroyed by the test.

### Closing out Phase 3 - honest status
Checked ROADMAP.md's full "Done when" bar before declaring Phase 3 complete, rather than assuming step 5 finished the phase. Presented two real gaps to the user and got explicit direction (accept both as known limitations for now) before proceeding:

- **"Runs unattended 48+ hours"** — not achievable inside an interactive session. Not verified. The user is separately considering running it on their own EC2 instance while away for 2 days to confirm this for real — noted that this is the user's own infra choice outside the codebase, not something requiring any AWS-specific code/config now (that stays Phase 6 scope per CLAUDE.md).
- **"Kill the poller process — does it restart, or stay silently dead?"** — tested for real: started the real poller, killed its actual worker process mid-cycle, waited, confirmed no new process appeared and the log never showed cycle completion. **It stays dead.** No process supervisor exists in this phase's tech stack (Docker/Kubernetes are explicitly Phase 6, and nothing in Phase 3's tech list calls for PM2/systemd/etc. either). Accepted as a known limitation until a later phase's deployment work addresses it.

**The other two failure cases were tested for real, and testing them properly surfaced a genuine bug:**

- **"Simulate an SEC outage for an hour — does it catch up cleanly after?"** — Before testing, traced through `runPollCycle()`'s logic and found a real bug: the cursor (`poller_state.last_checked_at`) advanced unconditionally after every cycle, *even if every single company's submissions check had failed*. During a real outage this would silently and **permanently** skip the outage window forever — directly violating the roadmap's own "zero permanent gaps" bar. Fixed in [src/poller.ts](src/poller.ts): the cursor now only advances if zero companies failed their submissions check; any failure keeps the old cursor so the same window gets re-checked next cycle instead of silently lost. Companies whose *submissions check itself* fails now also feed into the same `failing_companies` quarantine tracking from step 4 (previously only ingestion failures did) — this means a company that's *specifically* and persistently broken eventually gets quarantined and stops blocking the cursor for everyone else, while a genuine shared outage (many/all companies failing at once) correctly blocks the cursor since no individual company hits the quarantine threshold from a single bad cycle. Added an optional `universeOverride` parameter to `runPollCycle()` (mirroring the same testability pattern already used for `TokenBucket`/`fetchWithRetry`) so this could be tested against 3 real companies instead of the full 196 — otherwise the test would take ~11 minutes just from real per-company retry/backoff overhead during the simulated outage. Verified: a total outage (real local `fetch` override, not a mock) → cursor does not advance; outage clears → next cycle succeeds, cursor advances, and it correctly finds the real filings that occurred during the (wide, deterministic) lookback window — proving the "catch up cleanly" behavior isn't just "doesn't crash" but actually recovers the missed data.
- **"A permanently malformed filing retried forever"** — already conclusively covered by step 4's testing (5 consecutive real failures → quarantined → subsequent attempts skipped, not retried forever).
- **"Deliberately skip one filing — confirm nightly reconciliation finds and backfills it"** — tested for real with the step-2 date-bug fix in place (one more genuine ~1.4GB download, deliberately justified given this is the one failure case that specifically needed the fixed comparison logic re-verified end-to-end): deleted Tesla's `filing_facts` rows entirely (simulating a fully-skipped filing), ran the real reconciliation job — it found **exactly 2 discrepancies** (Tesla's `Revenues` and `NetIncomeLoss`, both correctly `stored=(missing)`) out of all 196 companies, and correctly restored both to their real values. The "exactly 2" is itself meaningful confirmation the date-bug fix holds: before that fix, the same kind of run had falsely flagged all 389 facts; now only the genuinely-deleted ones show up.

Also cleaned up a stray `reconciliation.ts` process left running in the background since step 2's testing (it just sat idle waiting for its 2 AM cron trigger — harmless, but sloppy to have left running).

**Phase 3 declared done** on this basis: 3 of 4 failure cases fully pass, the 4th (poller auto-restart) has a real, understood, user-accepted gap; all 5 build items complete and tested against real data throughout; the 48h unattended bar is explicitly deferred to the user's own optional follow-up test.
- [ ] Phase 0, step 5 — Parse + print most recent Revenues / NetIncomeLoss, with tag fallback
- [ ] Phase 0, step 6 — Handle bad-CIK and no-XBRL-data cases without crashing
- [ ] Phase 0, step 7 — Verified against 5 real companies including a failure case
