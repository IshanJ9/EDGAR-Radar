node_modules/
dist/
build/
*.log
logs/
.cache/

.env
.env.*
!.env.example

# Jest coverage output (Phase 6, step 3) - not generated by default (`npm
# test` runs without --coverage), but a local `jest --coverage` run would
# create this directory and it should never be committed.
coverage/
